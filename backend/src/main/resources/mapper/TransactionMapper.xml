<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.financetracker.backend.mapper.TransactionMapper">

    <select id="selectByUserIdAndDateRange" resultType="com.financetracker.backend.entity.Transaction">
        SELECT *
        FROM transaction
        <where>
            user_id = #{userId}
            <if test="startDate != null">
                AND transaction_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND transaction_date &lt;= #{endDate}
            </if>
        </where>
        ORDER BY transaction_date DESC
    </select>

    <select id="selectByFilter" resultType="com.financetracker.backend.entity.Transaction">
        SELECT *
        FROM transaction
        <where>
            user_id = #{userId}
            <if test="type != null">
                AND type = #{type}
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
            <if test="startDate != null">
                AND transaction_date &gt;= #{startDate}
            </if>
            <if test="endDate != null">
                AND transaction_date &lt;= #{endDate}
            </if>
        </where>
        ORDER BY transaction_date DESC
    </select>

    <resultMap id="CategoryStatisticsMap" type="com.financetracker.backend.dto.CategoryStatistics">
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="total" column="total"/>
        <result property="count" column="count"/>
    </resultMap>

    <select id="selectCategoryStatistics" resultMap="CategoryStatisticsMap">
        SELECT t.category_id, c.name AS category_name,
            SUM(t.amount) AS total, COUNT(*) AS count
        FROM transaction t
        LEFT JOIN category c ON t.category_id = c.id
        <where>
            t.user_id = #{userId}
            <if test="type != null">AND t.type = #{type}</if>
            <if test="startDate != null">AND t.transaction_date &gt;= #{startDate}</if>
            <if test="endDate != null">AND t.transaction_date &lt;= #{endDate}</if>
        </where>
        GROUP BY t.category_id, c.name
        ORDER BY total DESC
    </select>

    <select id="selectMonthlySummary" resultType="com.financetracker.backend.dto.MonthlySummary">
        SELECT
            COALESCE(SUM(CASE WHEN type = 'income' THEN amount ELSE 0 END), 0) AS totalIncome,
            COALESCE(SUM(CASE WHEN type = 'expense' THEN amount ELSE 0 END), 0) AS totalExpense,
            COALESCE(SUM(CASE WHEN type = 'income' THEN amount ELSE -amount END), 0) AS net
        FROM transaction
        WHERE user_id = #{userId}
        AND YEAR(transaction_date) = #{year}
        AND MONTH(transaction_date) = #{month}
    </select>

</mapper>
